services:
  - type: web
    name: warung-kopi
    runtime: node
    buildCommand: npm install
    startCommand: node app.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
